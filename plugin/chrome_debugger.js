(function() {
	/*
		This file uses the Chrome debugger:
		https://developer.chrome.com/devtools/docs/protocol/1.1/index
		
		1) Start the app with --remote-debugging-port=9222
		
		2) Connect to http://localhost:9222/json
		
		3) Connect to "webSocketDebuggerUrl"
		
		4) Prosper!
		
		PS. Reloading the app will disable the debugger! And we'll have to reconnect.
		
		Self debugging
		--------------
		* Check if the app is running with the --remote-debugging-port option.
		* Connect to the debugger
		* Open a console file to display console messages
		* Enable Setbreakpoint in editor menu
		* When a breakpoint is fired, collect all variables, alert, then show the file and scroll to the breakpoint.
		* Remove breakpoint.
		* When mouse-over a variable, show the variable collected.
		
		* If an error is thrown, show an dialog: "Jump to error", if yes:
		* open the file, scroll down to the line and show the error message.
		
		
		
	*/
	
		})();
		