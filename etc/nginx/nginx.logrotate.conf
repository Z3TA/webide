# sudo cp etc/nginx/nginx.logrotate.conf /etc/logrotate.d/

/home/*/log/*.log {
  daily
  dateext
  dateformat -%Y-%m-%d
  missingok
  rotate 12
  
  postrotate
    [ ! -f /var/run/nginx.pid ] || kill -USR1 `cat /var/run/nginx.pid`
  endscript
	
}
